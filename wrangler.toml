# Cloudflare Workers — site statique Next.js (Workers Sites)
# Build : npm run build  puis  npx wrangler deploy
# Le dossier out/ est généré par "next build" (output: 'export').

name = "fr"
main = "worker.js"
compatibility_date = "2024-01-01"

[site]
bucket = "./out"
